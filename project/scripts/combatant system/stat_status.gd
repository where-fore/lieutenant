extends Node2D

var statDictionary: Dictionary[String,int] = {}

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	#statDictionary[Stats.health] = 100
	#print(statDictionary)
	#statDictionary[Stats.attack] += 100
	#print(statDictionary)
	pass
	
