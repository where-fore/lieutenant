extends Scenario

func _init() -> void:
	text_pages = [
		"You find a quiet clearing in the denser forest. A good spot for a camp or an outpost, you think.\n\nThere seems to be nothing else of note here, but you can't shake the feeling you're being watched. Probably just the crows."
	]
	
	display_blurb = "Forest Clearing"
	display_sprite = load("res://sprites/question.png")
